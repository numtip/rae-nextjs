# Log — 2026-02-21

## Summary
Completed the full dynamic Green Office dashboard system in Joomla 6.  
Resolved all blocking errors and implemented auto-sync between the custom field and article content emoji.

## Changes

### 1. Fixed j6_assets nested-set tree (moveByReference error)
- **Problem**: 40 SQL-inserted articles/categories had `lft=0, rgt=0` in `j6_assets`.
- **Fix**: Generated `/tmp/fix_assets.py` → `/tmp/fix_assets.sql`:
  - Shifted all nodes ≥ position 85 by +80 to make room.
  - Updated `com_content` asset (id=8) `rgt` to 164.
  - Assigned sequential `lft`/`rgt` pairs (84–163) to all 40 broken assets, `level=2`, `parent_id=8`.
- **Result**: No more `moveByReference()` error on article save.

### 2. Custom field "สถานะหลักฐาน" visibility fix
- Created `j6_assets` entries for `com_fields.fieldgroup.1` and `com_fields.field.1`.
- Fixed `j6_fields.fieldparams` Unicode encoding (direct UTF-8 emoji instead of escaped sequences).
- Updated `j6_fields_groups.asset_id` and `j6_fields.asset_id` to point to new asset records.
- **Result**: "Green Office สถานะ" Tab with dropdown now appears in article editor.

### 3. Plugin: plg_content_go_statussync (NEW — ext_id=10177)
- **File**: `/var/www/html/plugins/content/go_statussync/plg_go_statussync.php`
- **Trigger**: `onContentAfterSave` for articles in catid 19–25.
- **Logic**:
  1. Read `j6_fields_values` for `field_id=1` (go-status) on saved article.
  2. Map: `0→⏳`, `1→✅`, `2→🔄`.
  3. Regex-replace all `<td ... text-align: center>EMOJI</td>` cells in `introtext`/`fulltext`.
  4. Clear `com_content` cache.
- **Result**: Changing the custom field and saving auto-updates emoji in article tables.

### 4. Module mod_go_dashboard (confirmed working)
- `{loadmoduleid 1}` shortcode in article 25 renders live dashboard.
- PHP reads `j6_fields_values` at render time — no stale data.

### 5. Plugin plg_content_go_clearcache (confirmed working, ext_id=10176)
- Clears `com_content` cache on every article save.

## Tests / Verify
- [x] `j6_assets` broken count = 0 (`SELECT COUNT(*) FROM j6_assets WHERE lft=0 AND parent_id=8` → 0).
- [x] `go_statussync` extension registered and enabled (ext_id=10177).
- [x] Plugin file present at `/var/www/html/plugins/content/go_statussync/`.
- [ ] **PENDING**: Open article in Admin → change field → Save → confirm emoji changes on frontend.
- [ ] **PENDING**: Confirm Dashboard % updates after field change.

## Next Step
User should open any article (e.g., 2.1 การอบรม), go to Tab "Green Office สถานะ", change "สถานะหลักฐาน" to ✅ มีหลักฐานแล้ว, Save, then verify:
1. The ⏳ in the article table became ✅.
2. The Dashboard category bar shows increased progress.
